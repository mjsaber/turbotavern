<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Framework DeviceDefault theme (the activities extend android.app.Activity, not AppCompat). -->
    <style name="Theme.TurboTavern" parent="@android:style/Theme.DeviceDefault.DayNight">
        <item name="android:colorPrimary">@color/tt_amber</item>
        <item name="android:colorAccent">@color/tt_amber</item>
    </style>
</resources>
